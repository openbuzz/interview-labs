server {
    listen 8081;
    ssi on;
    root /usr/share/nginx/sim;
    location / { try_files $uri /ide.html; }
}

server {
    listen 8082;
    ssi on;
    root /usr/share/nginx/sim;
    location / { try_files $uri /term.html; }
}
