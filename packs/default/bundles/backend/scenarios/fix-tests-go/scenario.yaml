# yaml-language-server: $schema=https://raw.githubusercontent.com/openbuzz/interview-labs/main/spec/pack/v1/scenario.schema.json

name: Fix the failing Go tests
description: Make the Go test suite pass without changing the tests.
