# String Utilities

A small Go package with string helper functions. The tests are correct, but the implementation has bugs — two of the five tests are failing.

Read the test output, find the bugs, and fix the implementation so that all tests pass.

## Run tests

```bash
go test -v ./...
```

## Verify

All five tests should pass:

```
ok  	stringutil	0.001s
```
