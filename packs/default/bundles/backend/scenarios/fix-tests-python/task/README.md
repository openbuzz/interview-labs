# String Utilities

A small Python module with string helper functions. The tests are correct, but the implementation has bugs — two of the five tests are failing.

Read the test output, find the bugs, and fix the implementation so that all tests pass.

## Run tests

```bash
python -m unittest -v
```

## Verify

All five tests should pass:

```
Ran 5 tests in 0.001s

OK
```
