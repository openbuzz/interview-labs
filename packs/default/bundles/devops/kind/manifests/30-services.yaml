---
# Broken service for the catalog namespace.
# Two issues: wrong selector and wrong targetPort.
apiVersion: v1
kind: Service
metadata:
  name: product-api-svc
  namespace: catalog
spec:
  type: ClusterIP
  selector:
    app: catalog-api
  ports:
    - port: 80
      targetPort: 8080
