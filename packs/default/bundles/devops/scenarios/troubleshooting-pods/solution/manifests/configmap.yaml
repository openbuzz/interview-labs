apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-config
  namespace: payments
data:
  app.conf: |
    server {
        listen 80;
        server_name localhost;
        location / {
            return 200 'healthy\n';
            add_header Content-Type text/plain;
        }
    }
